{
  "name": "uri-path-manager",
  "version": "2.1.0",
  "author": "Keith Jackson",
  "license": "MIT",
  "private": false,
  "keywords": [
    "stub",
    "window",
    "global",
    "wrapper",
    "path"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/ministryotech/path-manager.git"
  },
  "main": "path-manager.js",
  "devDependencies": {
    "eslint": "^8.56.0",
    "eslint-config-defaults": "^9.0.0",
    "uglify-js": "^3.17.4"
  },
  "dependencies": {
    "require": "^2.4.20",
    "window-wrapper": "^2.1.0"
  },
  "scripts": {
    "lint": "eslint src/*.js --fix",
    "ensure-dir": "mkdir ./dist",
    "clean": "rm -rf ./dist",
    "uglify": "uglifyjs src/path-manager.js -m -o ./dist/path-manager.js && uglifyjs src/path-manager.js -m -c -o ./dist/path-manager.min.js",
    "build": "npm run clean && npm run ensure-dir && npm run uglify && cp README.md ./dist/ && cp package.json ./dist/",
    "publish-to-npm": "npm publish dist/",
    "build-tar-for-github": "tar -czvf path-manager.tar.gz dist/",
    "release": "npm run lint && npm run build && npm run publish-to-npm && npm run build-tar-for-github"
  }
}
